# --- Composer ---
/vendor/
/composer.phar

# Mantém composer.lock versionado (bom para apps)
# (não ignore o composer.lock)

# --- Env/Secrets ---
/.env
/.env.*
!.env.example

# --- Logs / Cache / Artefatos ---
/storage/*
!/storage/.gitkeep
/storage/logs/*
!/storage/logs/.gitkeep
/storage/tmp/*
!/storage/tmp/.gitkeep

# PHPUnit / Cobertura
.phpunit.result.cache
/coverage/
/build/

# Ferramentas de qualidade
.php-cs-fixer.cache
# (phpstan-baseline.neon pode ser versionado; não ignore)

# --- Uploads & arquivos gerados pelo app ---
/public/uploads/*
!/public/uploads/.gitkeep

# (Se tiver outros diretórios de upload, repita o padrão acima)

# --- IDE / Editor ---
/.vscode/
/.idea/
/nbproject/
*.swp
*.swo
*.bak

# --- SO ---
.DS_Store
Thumbs.db
Desktop.ini

# --- Frontend (se usar no futuro) ---
/node_modules/

